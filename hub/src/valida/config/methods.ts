/**
 * Métodos de validación que ofrece el wizard (paso 2) y que
 * buildValidaState mapea a los valores del enum DocumentGroupName del backend.
 *
 * No es data de ejemplo: es la lista canónica que define qué grupos de
 * documentos puede adjuntar el usuario.
 */
export const METHODS = [
  'Linealidad', 'Precisión del sistema', 'Precisión del método',
  'Precisión intermedia', 'Exactitud', 'Estabilidad solución',
  'Estabilidad fase móvil', 'Robustez',
];
